{
  "name": "ecommerce-js",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watchAll"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}